const state = {};

const mutations = {};

const actions = {
  socket_winners({ commit }, { _winningNumber, _winners, _rewardAmount }) {}
};

const getters = {};

export default {
  state,
  mutations,
  actions,
  getters
};
