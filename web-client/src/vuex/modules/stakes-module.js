const state = {};

const mutations = {};

const actions = {
  socket_stake({ commit }, data) {},
  socket_game({ commit }, data) {},
};

const getters = {};

export default {
  state,
  mutations,
  actions,
  getters,
};
